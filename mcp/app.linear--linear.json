{
    "serverId": "app.linear/linear",
    "date": "2026-06-10",
    "installConfigs": [
        {
            "tool": "theia-ide",
            "config": {
                "servers": {
                    "linear": {
                        "serverUrl": "https://mcp.linear.app/sse"
                    }
                }
            },
            "instructions": "Remote Linear MCP server. No API key is required: on first connection Theia opens a browser to authorize access to your Linear workspace via OAuth, then stores the resulting token automatically. Provides tools to manage Linear issues, projects, and teams.",
            "installUrl": "theia://install-mcp?id=app.linear%2Flinear"
        },
        {
            "tool": "theia-ide-next",
            "config": {
                "servers": {
                    "linear": {
                        "serverUrl": "https://mcp.linear.app/sse"
                    }
                }
            },
            "instructions": "Remote Linear MCP server. No API key is required: on first connection Theia opens a browser to authorize access to your Linear workspace via OAuth, then stores the resulting token automatically. Provides tools to manage Linear issues, projects, and teams.",
            "installUrl": "theia-next://install-mcp?id=app.linear%2Flinear"
        }
    ]
}